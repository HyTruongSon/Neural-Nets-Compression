function [] = test_FFT()
    nRows = 30;
    nCols = 20;
    A = rand(nRows, nCols);
    F_A = fft(reshape(A, nRows * nCols, 1));
    for radius = 1 : nRows * nCols / 2
        start = 
        f_A = F_A;
        f_A(i + 1 : end) = 0.0;
        A_ = reshape(ifft(f_A), nRows, nCols);
        fprintf('Frequency %d: Norm-1 error = %.6f\n', i, sum(sum(abs(A - A_))));
    end
end